import { LoginForm } from "@/components/login-form";

export default function LoginPage() {
  return (
    <main className="app-screen flex min-h-screen items-start justify-center bg-background px-5 py-6 md:items-center">
      <LoginForm />
    </main>
  );
}
